# Changelog

## Unreleased

- 2026-07-22: Connected Admin Approve and publish to anonymous Works and public creator profiles. Added opaque stable creator slugs, strict published-only Works/creator/status RPCs, derivative-only anonymous Storage reads, server-side DTO allowlists and short-lived signing, `/creators/{slug}`, Viewer creator links, and a Dashboard public-profile capability. Configured Supabase empty/error responses now fail closed without sample or IndexedDB fallback; MT Web API DTOs exclude originals, owner IDs, explicit Storage fields, review evidence, and private GPS/EXIF. Reviewer actions remain non-publishing, while only Admin/Super Admin+AAL2 can publish. Static, secret-free HTTP, existing Auth/Workspace/Review/Dashboard regression, and 1440/1024/390 responsive checks pass; rollback-only development PostgreSQL acceptance is included as the deployment gate. Provider locators inside Supabase signed URLs and the anonymous delivery RPC remain documented rather than treated as an anonymity boundary.
- 2026-07-22: Unified Home, Works, About, Lightbox, Contact, and the protected creator profile around one fixed full-width header and removed the public left rail plus its reserved layout width. Added an accessible mobile navigation controller and public Sign In/account switching, rebuilt Works as search + underlined Type/Ratio filters + title/count/data state + responsive full-width masonry, and restrained `/dashboard` to a cover-led identity composition with an overlapping avatar, quiet profile facts, and the existing server-authoritative Overview/My works behavior. Existing search, URL filters, Viewer, Lightbox, account menu, cover chooser, Dashboard states, routes, and data boundaries remain in place; browser acceptance passed at 1440x900, 1024x768, and 390x844 without horizontal overflow.
- 2026-07-22: Expanded `/dashboard` into the full-width protected personal profile: all signed-in avatars now enter this page, the cover-led identity area exposes an explicit Edit personal information action, and Overview/My works retain the server-authoritative work summary. Account Settings now provides ten creator fields across Identity, Work, Location, About, and Links. A transaction-wrapped profile migration and `GET/PATCH /api/me/profile/cover` allow selecting or removing only the current owner's ready, current-policy scanner-clean display/thumbnail derivative; strict DTO projection, CSRF, recovery/inactive/Admin AAL guards, secret-free boundary tests, and rollback-only real-database coverage are included. The subsequent public-delivery slice now exposes the public profile after the first published work.
- 2026-07-22: Removed the homepage left rail so the photography hero and top navigation use the full viewport. Shortened the hero into regular flow capped at 92svh desktop and 82svh mobile so Selected Works remains visible in the first viewport; reduced marquee height, changed Statement to a stable two-column desktop/single-column mobile layout, and kept all Statement content readable before intersection enhancement. Signed-in homepage navigation now leads to Dashboard. Fresh 1440x900 and 390x844 visual checks show no blank section or horizontal overflow.
- 2026-07-22: Completed the Upload Studio Trash/Restore slice with an owner-scoped trashed-Draft read model, a read-only Trash view, explicit restore progress/error states, and automatic Inbox fallback when the original Folder was deleted. Secret-free API and browser acceptance cover CSRF, repeat-restore failure, 1440px/390px layout, horizontal overflow, fixed-header overlap, and console errors; no hard-delete action is exposed.
- 2026-07-22: Added the protected user Dashboard at canonical `/dashboard`: server-aggregated status counts, priority attention, recent private signed previews, review activity, truthful storage usage/capability states, profile identity, and Overview/My works views with loading, empty, permission, error, and retry handling. Signed-in avatars now open `/dashboard` on Home and every internal workspace, while the personal profile owns the explicit edit link; internal headers keep a separate keyboard-accessible account-menu button for Dashboard, Workspace, role-aware Review, Account Settings, and CSRF-protected Sign out. The transaction-wrapped aggregate RPC, strict server DTO projection, owner-prefixed thumbnail signing, static contract, and secret-free fake-provider HTTP boundary are wired into deployment preflight and CI.
- 2026-07-22: Passed the original rollback-only real PostgreSQL Dashboard/Trash acceptance with nine markers. The expanded Dashboard/Trash/creator-profile gate now requires twelve markers and adds the protected profile fields, cover eligibility, exact helper/exposed-RPC ACLs, owner isolation, inactive/recovery/stacked-role guards, transaction rollback, and independent fixture cleanup.
- 2026-07-22: Refined the Admin Review workflow for dense desktop and mobile use: mobile Queue/Detail now switch without scrolling through the full submission while preserving the deep link and active selection; queue controls have unique descriptive accessible names; evidence, history, checklist, and form typography/touch targets are legible; checklist failures focus and describe the first missing policy item through an assertive alert; desktop media keeps a natural sticky column and a non-obscuring Review decision shortcut. Secret-free validator/boundary checks and 1440x1000/390x844 fake-provider visual probes pass. Real disposable multi-identity browser acceptance also passes: Reviewer A claims the item, Reviewer B is denied cross-assignment access, Request Changes succeeds, an Admin at AAL2 approves, all three private asset variants load, responsive/focus/dialog/console checks stay clean, sessions close, and fixtures are removed.
- 2026-07-20: Implemented and deployed the Phase 3 Supabase Review Queue/Detail and decision boundary: scoped Reviewer claims, Admin+AAL2 history access, self-review denial, current-scan private assets, optimistic concurrency, immutable idempotent result replay, notifications, and append-only audit evidence. Rollback-only role/AAL/RLS/Storage/CAS/idempotency tests, three synchronized two-session database races, and secret-free fake-provider desktop/mobile browser acceptance passed. The real disposable Reviewer/Admin multi-identity browser gate subsequently passed on 2026-07-22; the later public-delivery slice now exposes the guarded Admin publish action.
- 2026-07-17: Implemented Phase 2F trusted asset scanning: restricted leased scan jobs and append-only events; `SKIP LOCKED` claim, token-bound idempotent completion, bounded retry and expiry handling; redirect-safe private Storage streaming with byte/checksum/magic validation; credential-scrubbed ClamAV and isolated Pillow full decode with EXIF, multi-frame, decompression-bomb, timeout and resource boundaries; fail-closed current-policy readiness; hash-locked dependencies; loopback and rollback-only database state-machine tests; CI and development DB deployment validation. The Web server/browser never receive the scanner credential. A persistent development Worker is not yet provisioned, so current jobs remain queued rather than being marked clean.
- 2026-07-16: Implemented Phase 2E Submit Readiness and Submit for Review: five server-authoritative readiness checks with pending polling; explicit confirmation, `expected_version`, and UUID idempotency; immutable image-version, review, readiness, and asset snapshots; transactionally consistent workflow, notification, and audit writes; strict API response projection; registered Storage object retention; successful Draft removal and submitted-state locking in Upload Studio. Also fixed the legacy Review Center false-dirty leave warning by comparing only editable Homepage fields and making `beforeunload` read existing dirty state. Real uploaded assets still begin with `scan_status=pending`, no user quota policy is claimed, and the Supabase Review Queue remains the next slice.
- 2026-07-16: Implemented and deployed Phase 2D Draft autosave and optimistic concurrency: 900 ms debounced serialized saves with a retained manual Save command; Saving, Saved, Error, and Conflict states; `lock_version`/`expected_version` compare-and-swap updates; HTTP 409 recovery that preserves local form values and offers `Reload Server Draft`; versioned Trash including dirty-save-then-Trash ordering; Folder-delete version bumps and serialized Folder assignment; removal of post-commit asset signing from Draft PATCH responses; and authenticated RPC execution moved from the old update/Trash functions to their versioned replacements.
- 2026-07-16: Delivered Phase 2C Draft compliance metadata: grouped Work Details and Accessibility/Rights editing, conditional Model Release UI, owner-scoped persistence for Alt Text, copyright, releases, rights, AI and sensitive-content disclosures, database/RPC constraints, and invalid-input integration coverage.
- 2026-07-16: Added the same compact public rail to Home, Works, About, Contact, and Lightbox, including the protected Upload entry, active-page states, mobile fallback navigation, and a static navigation contract. Workspace authorization now retries transient provider failures and distinguishes upstream failure (502) from an inactive account (403).
- 2026-07-16: Unified the Works/Upload/Review/Account rails, restored the protected Upload entry on Works, and redesigned Upload Studio around a progressive two-column empty state, on-demand Draft editor, truthful loading skeletons, restrained separators, and verified desktop/mobile layouts.
- 2026-07-16: Delivered Phase 2B upload resilience: a stable two-worker queue with per-task Cancel/Retry/Remove controls, abort-aware signed uploads, server-authoritative cancellation, partial private-object cleanup through Supabase Storage, responsive queue states, development migration verification, and expanded boundary coverage.
- 2026-07-15: Delivered Phase 2A of the private Upload Workspace: owner-scoped Supabase Folders/Drafts, three private Storage buckets, signed upload intents, server-only business writes, soft-delete Trash, read-only offline cache, development migration verification, and boundary tests in CI.
- 2026-07-15: Completed the protected Account Settings slice with owner-only Profile/Preferences updates, current-session capability reporting, provider-supported bulk revocation, Admin AAL2 and recovery guards, incremental Supabase migration deployment, and secret-free integration coverage.
- 2026-07-14: Added the complete Register/Verify/Sign In/Sign Out/Forgot/Reset foundation, HttpOnly session rotation, CSRF/Origin enforcement, protected Workspace routing, Admin MFA, and private legacy upload-asset boundaries.
- 2026-06-17: Connected the internal Manage editor to the local SQLite archive for existing seeded work metadata and grouped tags via `PATCH /api/archive/images/{id}`.
- 2026-06-17: Connected the public Works Archive to a read-only local SQLite API (`GET /api/archive/images`) with browser fallback to local samples and IndexedDB records.
- 2026-06-17: Added a database validation workflow for the local SQLite archive seed, including schema/view, foreign key, asset, tag JSON, ratio, and local path checks in CI.
- 2026-06-15: Reworked the Works Archive enlarged viewer into a full-screen image detail system with fit/actual-size zoom, a side information rail, and grouped tag rendering; expanded derived tags to include subject categories such as landscape, house/building, architecture, animals, objects, coast/water, stone, and surface/pattern.
- 2026-06-15: Compacted the Works Archive top navigation/search/filter chrome and synchronized the internal Manage tag editor with the same seven grouped tag taxonomy used by the public viewer and SQLite seed.
- 2026-06-14: Added a local SQLite Works Archive verification database and seed script that imports sample image metadata, assets, grouped tags, taggings, analysis rows, and an archive-featured collection from `archive-data.js`.
- 2026-06-10: Added the Works Archive work viewer with enlarged display images, catalog-style metadata, grouped tag visualization, keyboard navigation, scroll locking, and deferred database fields for curatorial notes, artist statements, series, and tag groups.
- 2026-06-08: Added the `4:3` Works Archive ratio category across filters, upload classification, local samples, and deferred database schema.
- 2026-06-08: Added local upload compression and multi-version asset records for Works Archive uploads, including original, display, thumbnail, and square slice assets aligned with the deferred database schema.
- 2026-06-08: Refined the Works Archive visual system with smaller radii, serif/sans typography roles, thin-line functional icons, grouped filter/manage controls, and icon-based Arrange actions.
- Moved Selected Works above the homepage Statement and rebuilt Statement as four image/text moments with per-moment entrance animation.
- Added Works Archive Arrange mode with drag ordering, Earlier/Later controls, local order saving, and refresh recovery.
- Added the Contact Artist page linked from the homepage and Works Archive page.
- Removed the local Messages inbox/API path; Contact now opens a mail draft and no longer stores local messages.

## v1.0.0 - 2026-06-07

- Created the first public project version for GitHub.
- Built the static MT Presence homepage with a sticky black-and-white to color hero transition.
- Added the Infinite Marquee Gallery for selected works.
- Added the Works Archive page with local image samples, upload support, ratio classification, Abstract/Concrete filters, and IndexedDB persistence.
- Added deferred PostgreSQL/Supabase database design files for future backend integration.
- Confirmed the database is not active in this version; the project currently runs as a static frontend.
